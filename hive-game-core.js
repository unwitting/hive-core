const { Board } = require('./lib/Board')
const { Game } = require('./lib/Game')
const { Player } = require('./lib/Player')
const { Move } = require('./lib/Move')

module.exports = { Board, Game, Player, Move }
